/**
 * Enumeration representing user roles.
 */
export enum Rol {
  USER = 'user',
  ADMIN = 'admin',
}

/**
* Enumeration representing possible states.
*/
export enum Estado {
  ACTIVO = 'activo',
  INACTIVO = 'inactivo',
  SUSPENDIDO = 'suspendido',
  
}